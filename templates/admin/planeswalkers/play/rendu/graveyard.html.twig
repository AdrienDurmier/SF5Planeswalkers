{% macro script() %}
    <script>
        {# Rendu du graveyard #}
        function renderGraveyard(datas, player_area){
            player_area.find('.graveyard-top-card').text(datas.graveyard.count);
            // Mise à jour de la carte du dessus du graveyard
            if (datas.graveyard.count > 0){
                player_area.find('.graveyard-top-card').css({'background-image':'url('+datas.graveyard.topCard.imageUrisPng+')'});
            }else{
                player_area.find('.graveyard-top-card').css({'background-image':'none'});
            }
        }
    </script>
{% endmacro %}